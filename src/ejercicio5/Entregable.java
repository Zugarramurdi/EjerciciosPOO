package ejercicio5;

// Interfaz Entregable
/* Interfaz con los metodos que pide el enunciado
  algunos no se llegan a ausar en el ejercicio los piden */
public interface Entregable {
    void entregar();
    void devolver();
    boolean isEntregado();
    void compareTo(Object a);
}
